{% if settings.enable_shop_by_tags_4 %}

<div class="filter-block  filter-custom filter-tag">
  {% assign tags = settings.shop_by_tags_4_list_tags | split: ',' %}
  <h5 class="filter-title">
    <span>{{ settings.shop_by_tags_4_title }}</span><a href="javascript:void(0)" class="clear" style="display:none" >x</a>
  </h5>
  <div class="filter-content">
   <ul class="box-list-stype sizes">
      {% for t in tags %}
      {% assign tag = t | strip %}
      {% assign tag_value = tag | handleize %}
      {% if current_tags contains tag %}
      <li class="li-size">
        <input type="checkbox" value="{{ tag_value | split: '|' | first }}" checked/>
        <label>{{ tag | split: '|' | first }}</label>
      </li>
      {% elsif collection.all_tags contains tag %}

      <li class="li-size">
        <input type="checkbox" value="{{ tag_value | split: '|' | first }}"/>
        <a href="javascript:void(0)" title="{{tag}}">{{tag}}</a>
      </li>
      {% endif %}

      {% endfor %}
    </ul>
  </div>
</div>
{% endif %}

{% if settings.enable_shop_by_tags_5 %}

<div class="filter-block  filter-custom filter-tag">
  {% assign tags = settings.shop_by_tags_5_list_tags | split: ',' %}
  <h5 class="filter-title">
    <span>{{ settings.shop_by_tags_5_title }}</span>
    <a href="javascript:void(0)" class="clear" style="display:none" >x</a>
  </h5>
  <div class="filter-content">
   <ul class="box-list-stype sizes">
      {% for t in tags %}
      {% assign tag = t | strip %}
      {% assign tag_value = tag | handleize %}
      {% if current_tags contains tag %}
      <li class="li-size">
        <input type="checkbox" value="{{ tag_value | split: '|' | first }}" checked/>
        <label>{{ tag | split: '|' | first }}</label>
      </li>
      {% elsif collection.all_tags contains tag %}

      <li class="li-size">
        <input type="checkbox" value="{{ tag_value | split: '|' | first }}"/>
        <a href="javascript:void(0)" title="{{tag}}">{{tag}}</a>
      </li>
      {% endif %}

      {% endfor %}
    </ul>
  </div>
</div>
{% endif %}


{% if settings.enable_shop_by_tags_2 %}

<div class="filter-block filter-custom filter-tag">
  {% assign tags = settings.shop_by_tags_2_list_tags | split: ',' %}
  <h5 class="filter-title">
    <span>{{ settings.shop_by_tags_2_title }}</span><a href="javascript:void(0)" class="clear" style="display:none" >x</a>
  </h5>
  <div class="filter-content">
    <ul class="box-list-stype">
      {% for t in tags %}
      {% assign tag = t | strip %}
      {% assign tag_value = tag | handleize %}
      {% if current_tags contains tag %}
      <li class="li-price active">
        <input type="checkbox" value="{{ tag_value | split: '|' | first }}" checked/>
        <label>{{ tag | split: '|' | first }}</label>
      </li>
      {% elsif collection.all_tags contains tag %}

      <li class="li-price">
        <input type="checkbox" value="{{ tag_value | split: '|' | first }}"/>
        <a href="javascript:void(0)" title="{{tag}}">{{tag}}</a>
      </li>

      {% endif %}
      {% endfor %}
    </ul>
  </div>
</div>
{% endif %}

